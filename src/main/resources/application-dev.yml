spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: DEBUG
    dev.whysoezzy: DEBUG
    org.springframework.web: DEBUG

app:
  sms:
    provider: console
  s3:
    enabled: false
